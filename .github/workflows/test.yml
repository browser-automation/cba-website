name: CI tests
on: push
jobs:
  test:
    runs-on: ubuntu-16.04
    steps:
    - uses: actions/checkout@v2
    - name: Use Node.js
      uses: actions/setup-node@v2-beta
      with:
        node-version: 12.x
    - run: npm i
    - run: npm test
      env:
        CI: true
